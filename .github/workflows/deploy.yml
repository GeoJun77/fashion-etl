# .github/workflows/deploy.yml
# Deployment workflow — disabled, using Streamlit Cloud instead

name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deployment handled by Streamlit Cloud"